import React, { Component } from 'react';
import Tmp from '../tmp/tmp';

export default class BugList extends Component {
    render() {
        return (
            <div className="bug-list border-right" ><Tmp /></div>
        );
    }
}